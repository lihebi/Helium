# Segment Code Selection Method

When using `annotation` method,
whenever you add `@HeliumStart` and `@HeliumStop`,
Remember to rebuild ctags using `helium --ctags <folder>`.

Currently only one method at a time is supported.
